{
  "name": "@accelerate-core/worker",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "test": "node -e \"console.log('apps/worker: no tests yet')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@accelerate-core/connectors": "workspace:*",
    "@accelerate-core/connector-joshuaproject": "workspace:*",
    "@accelerate-core/firestore": "workspace:*",
    "@accelerate-core/shared": "workspace:*",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.2"
  }
}

