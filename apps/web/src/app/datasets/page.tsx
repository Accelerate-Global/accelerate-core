export default function DatasetsPage() {
  return (
    <div className="card">
      <h1>Datasets</h1>
      <p className="muted">Placeholder list. First dataset id: pgic_people_groups.</p>
      <p className="muted">V1: dataset versions are created by the worker as BigQuery tables per version.</p>
    </div>
  );
}
