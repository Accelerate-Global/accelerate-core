{
  "name": "@accelerate-core/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @accelerate-core/shared build && next build",
    "dev": "next dev -p 3000",
    "lint": "eslint .",
    "test": "node --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "next start -p 3000"
  },
  "dependencies": {
    "@accelerate-core/shared": "workspace:*",
    "firebase": "^10.14.1",
    "next": "15.5.7",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/react": "19.2.13",
    "@types/react-dom": "19.2.3"
  }
}
